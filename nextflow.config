process {
    container = "satijalab/azimuth:0.4.6"
    memory = { 32.GB * task.attempt }
    cpus = { 4 * task.attempt }
    errorStrategy = 'retry'
    maxRetries = 2
}

params {
    input = false
    reference = "pbmcref"
}